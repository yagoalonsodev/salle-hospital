# Diario IA — 2026-05-06

Herramienta: **Cursor**

---

### Entrada 001 — Día 6: API Flask, pacientes, UI radiografías e integración ML

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-06 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | [api-predict.md](../../specs/api-predict.md), [api-pacientes.md](../../specs/api-pacientes.md) |
| BACKLOG | D6-01 … D6-05 (hecho) |

#### Prompt del usuario (consolidado)

```
Día 6: API Flask con /upload y /predict, integración ResNet50, UI web, pacientes con
0..N radiografías, nombres en lugar de IDs, datos en BD/MinIO sin placeholders, galería
por paciente en pestaña Radiografías, resumen con study_id y predicción, commit del
6 de mayo y actualizar skills SDD y diario IA.
```

#### Respuesta de la IA (resumen)

1. **API Flask** (`api/app/`): capas routes → services → repositories; Gunicorn en Docker.
2. **ML** (`ml/app/inference.py`): carga `rx_resnet50_v1.h5`, endpoint FastAPI interno.
3. **Flujo upload**: validación → MinIO → estudio `clinical` → inferencia → `predictions`.
4. **Pacientes**: CRUD, `display_name`, FK `sites`, migraciones Postgres.
5. **UI**: 3 pestañas; galería RX; resumen con ID + predicción; fix CSS paneles vacíos.
6. **Skills y BACKLOG** actualizados; sesión SDD `2026-05-06-api-flask-ui.md`.

#### Archivos afectados

- `api/` (Flask completo), `ml/app/inference.py`, `ml/app/main.py`
- `infra/postgres/03-*.sql`, `04-*.sql`, `05-*.sql`, `01-init-salle-schema.sql`
- `docker-compose.yml`, `docs/specs/api-predict.md`, `api-pacientes.md`, `BACKLOG.md`
- `.cursor/skills/salle-sdd/`, `.cursor/skills/diario-ia/`

#### Reflexión

La IA implementó rápido el stack Día 6 respetando SDD (Flask único en `api/`, negocio en `services/`). Hubo iteración UX (nombres, galería, pestañas) y corrección de datos de prueba vs catálogo `sites`. El criterio clínico (FN neumonía) sigue documentado en Día 5; la API expone la predicción operativa ResNet50.

#### Commit

- _(hash)_ — `feat(api): Día 6 — Flask, pacientes, UI radiografías e integración ML` (2026-05-06)

---
