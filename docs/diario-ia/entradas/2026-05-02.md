# Diario IA — 2026-05-02

Herramienta: **Cursor**

---

### Entrada 001 — Datos raw, CSV clínico y spec pipeline

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-02 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | [pipeline-datos-ejemplo.md](../../specs/pipeline-datos-ejemplo.md) |

#### Prompt del usuario (optimizado)

```
Alinear datos con el enunciado: mover RX a data/raw/covid19_vs_pneumonia,
generar CSV clínico/operativo (pacientes, estudios, logs), preparar processed/,
mantener ml/models para SavedModel; redactar spec SDD y registrar en el diario IA.
```

#### Respuesta de la IA (resumen)

- Spec `docs/specs/pipeline-datos-ejemplo.md` (SDD).
- Movido dataset (6432 JPG, 3 clases) → `data/raw/covid19_vs_pneumonia/`.
- Script `scripts/build_clinical_data.py` → CSV en `data/raw/clinical/` + `manifest.csv`.
- `data/README.md`, carpetas `data/processed/`, backlog D2-02 hecho.

#### Reflexión

Tres clases ya cubiertas por el dataset COVID; CSV y rutas cierran huecos del enunciado (estructurado + no estructurado). Siguiente: D2-01 esquema Postgres y D2-03 ingesta.

---

### Entrada 002 — Esquema PostgreSQL y arquitectura de BD

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-02 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | [pipeline-esquema-db.md](../../specs/pipeline-esquema-db.md) |

#### Prompt del usuario (optimizado)

```
Crear esquema PostgreSQL; documentar arquitectura de la BD; registrar sesión
en flujos @salle-sdd (spec antes de código) y @diario-ia (entrada con prompt optimizado).
```

#### Respuesta de la IA (resumen)

- Spec SDD `pipeline-esquema-db.md`.
- DDL `infra/postgres/01-init-salle-schema.sql`: `patients`, `studies`, `predictions`, `pipeline_runs`, `pipeline_events`, `data_quality_issues`, `alerts` + vistas.
- Guía `docs/database-architecture.md` (ER mermaid, MinIO, mapeo CSV).
- Compose actualizado; BACKLOG D2-01 hecho.
- Sesiones ejemplo en `.cursor/skills/*/sessions/2026-05-02-esquema-db.md`.

#### Archivos afectados

- `infra/postgres/01-init-salle-schema.sql`
- `docs/database-architecture.md`, `docs/specs/pipeline-esquema-db.md`
- `docker-compose.yml`, `docs/architecture.md`, `docs/specs/BACKLOG.md`
- `.cursor/skills/salle-sdd/SKILL.md`, `.cursor/skills/diario-ia/SKILL.md`

#### Reflexión

SDD evitó implementar tablas sin criterios: la spec enlaza CSV, enunciado y MinIO. El volumen Postgres ya creado exige `down -v` o `psql` manual para aplicar el DDL.

---

### Entrada 003 — Eliminación de patients.csv

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-02 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | [pipeline-datos-ejemplo.md](../../specs/pipeline-datos-ejemplo.md) |

#### Prompt del usuario (optimizado)

```
Eliminar el CSV de pacientes falsos; revisar organización de data/; actualizar sesiones
@salle-sdd y @diario-ia cuando esté hecho.
```

#### Respuesta de la IA (resumen)

- Eliminado `data/raw/clinical/patients.csv`.
- `build_clinical_data.py` genera solo `manifest.csv`, `studies.csv`, `pipeline_events.csv`.
- `studies.csv` mantiene `patient_id` opaco (1:1) para la FK en Postgres; tabla `patients` se llenará en ingesta D2-03.
- Docs y skills actualizados.

#### Archivos afectados

- `scripts/build_clinical_data.py`, `data/README.md`
- `docs/specs/pipeline-datos-ejemplo.md`, `docs/database-architecture.md`
- `.cursor/skills/*/sessions/2026-05-02-sin-patients-csv.md`

#### Reflexión

Separar “datos reales” (RX) de “metadatos inventados” (demografía) simplifica la memoria y evita confundir al evaluador. El encargo de CSV estructurado se cubre con `studies.csv`.

---

### Entrada 004 — Ingesta automática, validación y deduplicación (PySpark)

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-02 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | [pipeline-ingesta-imagenes-calidad.md](../../specs/pipeline-ingesta-imagenes-calidad.md) |

#### Prompt del usuario (optimizado)

```
Implementar carga automática de imágenes, validación y eliminación de duplicados;
registrar en @salle-sdd y @diario-ia.
```

#### Respuesta de la IA (resumen)

- Job `pipeline/jobs/ingest_validate_images.py`: escaneo recursivo JPG, validación JPEG, dedup por MD5, salida en `data/processed/manifest/`, subida MinIO, log Postgres opcional.
- `pipeline/jobs/db_log.py`, `pipeline/README.md`, spec y BACKLOG D2-04/D2-05 hechos.

#### Archivos afectados

- `pipeline/jobs/ingest_validate_images.py`, `pipeline/jobs/db_log.py`
- `pipeline/requirements.txt`, `pipeline/README.md`, `docker-compose.yml`
- `docs/specs/pipeline-ingesta-imagenes-calidad.md`, `docs/specs/BACKLOG.md`

#### Reflexión

Job por manifest + validación por partición (evita OOM en Spark). La subida MinIO en driver es aceptable para ~6k imágenes en MVP.

---

### Entrada 005 — Verificación Postgres + MinIO (encargo)

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-02 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | [pipeline-ingesta-imagenes-calidad.md](../../specs/pipeline-ingesta-imagenes-calidad.md) |

#### Prompt del usuario (optimizado)

```
Verificar que funcione MinIO y PostgreSQL según enunciado; actualizar @salle-sdd y @diario-ia.
```

#### Respuesta de la IA (resumen)

- Job con `RUN-VERIFY-IMG-01`: 6432 escaneadas → 6185 válidas únicas, 247 rechazadas (215 corrupt, 32 dup).
- Postgres: `pipeline_runs` ok, eventos `EVT-SCAN01`/`EVT-ING01`, `data_quality_issues` pobladas.
- MinIO: 100 objetos subidos (prueba `MAX_UPLOAD=100`); sin límite sube las ~6185.
- Script `scripts/verify_pipeline_integration.py`; skills y `pipeline/README.md` actualizados.

#### Corrección

Eventos con `run_id` largo en `event_id` → acortados a `EVT-SCAN01` / `EVT-ING01` (VARCHAR(16)).

#### Reflexión

Los 215 «corrupt» eran `.jpg` que no eran JPEG (p. ej. PNG); la validación era correcta. Corregido en la entrada 006 con `convert_rx_to_jpeg.py`.

---

### Entrada 006 — Normalización JPEG del dataset RX

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-02 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | [pipeline-ingesta-imagenes-calidad.md](../../specs/pipeline-ingesta-imagenes-calidad.md) |

#### Prompt del usuario (optimizado)

```
Pasar las fotos al formato adecuado; renombrar sessions SDD al día del commit (1–2 mayo), no 16 mayo.
```

#### Respuesta de la IA (resumen)

- `scripts/convert_rx_to_jpeg.py`: 215 convertidas, 6217 ya JPEG, 0 fallos.
- Regenerados `manifest.csv` y CSV clínico con `build_clinical_data.py`.
- Sessions renombradas según `git log`: solo `YYYY-MM-DD` si hay commit; pipeline en `pendiente-*.md`.

#### Archivos afectados

- `scripts/convert_rx_to_jpeg.py`, `data/processed/manifest/conversion_report.json`
- `.cursor/skills/salle-sdd/sessions/`, `.cursor/skills/diario-ia/sessions/`
- `docs/diario-ia/INDEX.md`, `data/README.md`

#### Reflexión

El pipeline no debe asumir que la extensión implica formato; un paso de normalización previo evita falsos «corruptos» y deja las 6432 imágenes listas para Spark y MinIO.

---
