# Diario IA — 2026-05-03

Herramienta: **Cursor**

---

### Entrada 001 — Preprocesado RX para entrenamiento (Día 3)

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-03 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | [pipeline-preprocesado-imagenes.md](../../specs/pipeline-preprocesado-imagenes.md) |

#### Prompt del usuario (optimizado)

```
Día 3: redimensionamiento, normalización, augmentation (rot, flip, zoom),
split train/validation/test con PySpark; justificación distribuida; dataset
procesado; commits legibles; actualizar skills SDD y diario.
```

#### Respuesta de la IA (resumen)

- Spec SDD + `docs/preprocess-distributed-justification.md`.
- `image_transforms.py` + `preprocess_images.py` (mapPartitions en cluster Spark).
- Imagen `practica-tocha-spark:3.5.3` con Pillow en workers.
- Salida: 6399 fuentes → 19446 muestras en `data/processed/features/v1/`.

#### Archivos afectados

- `pipeline/jobs/preprocess_images.py`, `pipeline/jobs/image_transforms.py`
- `infra/spark/Dockerfile`, `docker-compose.yml`, `pipeline/requirements.txt`
- `docs/specs/pipeline-preprocesado-imagenes.md`, `docs/specs/BACKLOG.md`

#### Reflexión

Los workers Spark no tenían PIL: obligó a extender la imagen del worker (decisión de infra coherente con el encargo). PySpark sigue siendo overkill en un portátil, pero unifica el pipeline y escala si crece el dataset.

#### Commits (Día 3)

- `93e7410` — spec preprocesado RX
- `fbc5d56`, `424619e`, `0818bd9` — Spark + transforms + job preprocess
- `67b3fc8` — BACKLOG, diario, informe preprocesado

---

### Entrada 002 — Skill seguridad y secretos fuera del código (SDD)

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-03 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | — (skill `.cursor/skills/salle-seguridad/`) |

#### Prompt del usuario (inferido de sesión SDD)

```
Revisar secretos hardcodeados, alinear skill salle-seguridad con SDD/diario,
y quitar valores por defecto inseguros del código Python (usar .env).
```

#### Respuesta de la IA (resumen)

- Skill `salle-seguridad`: checklist antes de commits, no pegar `.env` en diario.
- Refactor: credenciales solo vía `os.environ` / `env_utils.py`.
- Referencias en skills `salle-sdd` y `diario-ia`.

#### Archivos afectados

- `.cursor/skills/salle-seguridad/SKILL.md`
- `scripts/env_utils.py`, módulos que leían passwords por defecto
- `.cursor/skills/salle-sdd/SKILL.md`, `.cursor/skills/diario-ia/SKILL.md`

#### Reflexión

Encaja con entregable ético/legal: el diario no debe filtrar contraseñas aunque el `.env` local use valores de desarrollo.

#### Commits

- `8713668` — docs(security): skill salle-seguridad
- `b3a118d` — fix(security): quitar secretos del código Python

---
