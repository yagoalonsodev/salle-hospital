# Diario IA — 2026-05-05

Herramienta: **Cursor**

---

### Entrada 001 — Informe Día 5: comparativa 4 arquitecturas y conclusión memoria

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-05 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | [ml-entrenamiento.md](../../specs/ml-entrenamiento.md) |
| BACKLOG | D5-01, D5-02, D5-03 (hecho) |

#### Prompt del usuario (consolidado — una sesión)

```
Informe en notebook: entrenar baseline CNN, ResNet50, EfficientNetB0 y DenseNet121
con el mismo protocolo; estadísticas por modelo; comparativa final y elección honesta
para la memoria (DenseNet gana F1 macro; ResNet50 en producción por FN neumonía→sana,
accuracy y stack Docker). Añadir explicación de F1 macro en el notebook. Commit del
informe hecho, entrada en diario IA y sesión SDD del 5 de mayo.
```

#### Respuesta de la IA (resumen)

1. **Pipeline de entrenamiento:** `training_core.py` + scripts `train_*.py` y `train_compare_architectures.py`; rutas locales (`FEATURES_ROOT`) y `SKIP_IF_EXISTS` en notebook.
2. **Cuatro arquitecturas entrenadas/evaluadas** sobre `features/v1/`; comparativa en `architecture_comparison.json`.
3. **Notebook** `01_exploracion_arquitectura.ipynb`: celdas train+stats por modelo, gráficos F1/FN, celda **F1 macro** (definición para memoria), conclusión honesta (DenseNet vs ResNet50).
4. **Docs:** `resultados-entrenamiento-v1.md`, `evaluacion-clinica-v1.md`; BACKLOG y spec actualizados.
5. **Correcciones previas en la sesión:** path Docker vs local en kernel; `EXPORT_SAVEDMODEL=0` en macOS; monitor `watch_training.sh`.

#### Resultados clave (test)

| Modelo | Accuracy | F1 macro | FN neumonía→sana |
|--------|----------|----------|------------------|
| densenet121 | 93,2 % | **93,4 %** | 72 |
| resnet50 | **94,0 %** | 93,1 % | **37** |
| baseline_cnn | 87,7 % | 87,8 % | 118 |
| efficientnetb0 | 66,4 % | 26,6 % | colapso |

**Frase memoria:** *DenseNet121 obtuvo el F1 macro ligeramente superior; ResNet50 reduce los falsos negativos neumonía→sana y es el modelo operativo del sistema.*

#### Archivos afectados

- `ml/scripts/training_core.py`, `train_*.py`, `train_compare_architectures.py`
- `ml/notebooks/01_exploracion_arquitectura.ipynb`
- `ml/models/reports/architecture_comparison.json`, informes por arquitectura
- `docs/ml/resultados-entrenamiento-v1.md`, `docs/ml/evaluacion-clinica-v1.md`
- `docs/specs/ml-entrenamiento.md`, `docs/specs/BACKLOG.md`
- `.cursor/skills/salle-sdd/sessions/2026-05-05-informe-arquitecturas.md`

#### Reflexión

La IA aceleró el protocolo repetible (un core + cuatro scripts) y el informe ejecutable en notebook, alineado con el encargo (investigar arquitecturas, no solo accuracy). La decisión operativa requiere criterio humano: F1 macro no sustituye el análisis de FN clínicos. EfficientNetB0 documenta el riesgo de entrenamientos inestables sin abandonar el requisito de comparar alternativas.

#### Commit

- `a142a2c` — `feat(ml): informe Día 5 — comparativa 4 arquitecturas y conclusión memoria` (2026-05-05 16:30)

---
