# Diario IA — 2026-05-08

Herramienta: **Cursor**

---

### Entrada 001 — Día 8: Monitorización y calidad de datos

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-08 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | [monitorizacion-calidad-d8.md](../../specs/monitorizacion-calidad-d8.md) |
| BACKLOG | D8M-01 … D8M-04 (hecho) |

#### Prompt del usuario

```
Día 8 — Monitorización y calidad de datos: logging centralizado, validación
(imágenes corruptas, registros incompletos), alertas (fallos pipeline, errores
inferencia), healthchecks Docker, retry automático. Entregable: sistema robusto.
```

#### Respuesta de la IA (resumen)

1. `scripts/salle_logging.py` y `scripts/db_alerts.py` compartidos (watcher, pipeline, Airflow).
2. Ingesta Spark con try/except, conteos corrupt/incomplete y `alert_quality_batch`.
3. `data_quality_audit.py` + tarea Airflow al final del DAG.
4. Callback `on_failure_callback` en Airflow; retry MinIO en API.
5. Healthchecks: minio, spark, watcher (heartbeat), pipeline.

#### Iteraciones

| # | Qué pasó |
|---|----------|
| 1 | Primera versión del diario en formato breve; el usuario pidió commits formales después. |
| 2 | Verificación Airflow en sesión aparte: DAG `salle_rx_pipeline` sin errores de importación. |

#### Archivos afectados

- `scripts/salle_logging.py`, `db_alerts.py`, `data_quality_audit.py`, `airflow_callbacks.py`
- `pipeline/jobs/ingest_validate_images.py`, `scripts/image_watcher.py`
- `airflow/dags/salle_rx_pipeline.py`, `docker-compose.yml`, `api/app/services/minio_store.py`
- `ml/app/logging_config.py`, `infra/watcher/Dockerfile`
- `docs/specs/monitorizacion-calidad-d8.md`, `BACKLOG.md`

#### Reflexión

Centralizar alertas en `db_alerts.py` evita duplicar SQL entre API, pipeline y Airflow. El short-circuit del DAG cuando no hay `pending.flag` es correcto operativamente pero puede confundir en demo si se espera siempre ingesta Spark.

#### Commit

- _(este archivo)_ — `feat(ops): Día 8 — monitorización, calidad y robustez operativa`

---
