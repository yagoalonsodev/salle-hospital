# Diario IA — 2026-05-10

Herramienta: **Cursor**

---

### Entrada 001 — Día 10: preparación de presentación y demo (sin código nuevo)

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-10 |
| Herramienta | Cursor |
| Resultado | parcial |
| Spec SDD | — (entregable `enunciado.md` § Presentación; BACKLOG D10-01, D10-02) |

#### Prompt del usuario (consolidado)

```
Qué falta para cerrar la práctica; guion de presentación 10–15 minutos; cómo demostrar
el sistema (docker compose, UI hospital, dashboard, Airflow); confirmar que el encargo
está cubierto salvo la presentación oral.
```

#### Respuesta de la IA (resumen)

1. Revisión de cumplimiento vs `enunciado.md` y `docs/memoria-tecnica.md` §13.
2. **Guion propuesto** (~12–14 diapositivas): contexto hospital, arquitectura, pipeline Big Data, ML ResNet50, demo en vivo, ética (prompt injection), conclusiones.
3. **Ruta de demo sugerida:** `docker compose up -d --build` → http://localhost:8000 (subida RX) → http://localhost:8501 (dashboard) → http://localhost:8081 (DAG `salle_rx_pipeline`, tareas skipped sin `pending.flag`).
4. BACKLOG: único bloqueante explícito = presentación + ensayo desde cero.

#### Archivos afectados

- Sin commits ese día; trabajo en chat (guion) y revisión de `docs/specs/BACKLOG.md` mental.

#### Reflexión

La IA ayudó a estructurar la narrativa del entregable oral sin tocar código. Falta grabar/ensayar la demo real y subir diapositivas si el profesor las pide aparte del repo.

#### Siguiente paso

- Ensayar demo con `./scripts/verify_stack.sh` (añadido el 18 mayo).
- Presentación oral 10–15 min.

---
