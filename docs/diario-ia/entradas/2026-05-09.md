# Diario IA — 2026-05-09

Herramienta: **Cursor**

---

### Entrada 001 — Día 9: Documentación, memoria técnica y cierre documental

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-09 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | [documentacion-d9.md](../../specs/documentacion-d9.md) |
| BACKLOG | D9-01, D9-02, D9-03 (hecho) |

#### Prompt del usuario

```
Haz los commits del día 8 de mayo y ayúdame con Día 9 — Documentación + memoria técnica:
README completo, memoria técnica, diagramas, diario IA con prompts reales, iteraciones,
errores de la IA y cómo corregisteis. También riesgos Prompt Injection (mínimo 2) con
mitigaciones. Entregable: documentación casi final.
```

#### Respuesta de la IA (resumen)

1. Commit Día 8: `feat(ops): Día 8 — monitorización, calidad y robustez operativa`.
2. `docs/memoria-tecnica.md` — memoria integrada del proyecto.
3. `docs/diagramas.md` — arquitectura, secuencia upload, Airflow, ER, Docker, capas Flask.
4. `docs/etica.md` — RGPD conceptual, disclaimer clínico, **2 casos Prompt Injection**.
5. `README.md` corregido (Flask vs FastAPI, tabla días 1–9, enlaces).
6. Esta entrada y actualización de `INDEX.md` / `BACKLOG.md`.

#### Prompts reales usados en el proyecto (muestra representativa)

| Día | Prompt (extracto literal) |
|-----|---------------------------|
| 6 | «API Flask con /upload y /predict, pacientes 0..N radiografías, galería por paciente, commit del 6 de mayo» |
| 7 | «Dashboard Streamlit con métricas IA, alertas, gráficas, robustez: logging, healthchecks, retry ML» |
| 8 | «Monitorización y calidad: logging centralizado, imágenes corruptas, registros incompletos, alertas pipeline» |
| 8b | «Comprueba que los DAGs de Apache Airflow funcionan; usuario y contraseña» |
| 9 | «Commits día 8 + memoria técnica, diagramas, diario con prompts reales y prompt injection» |

#### Iteraciones y errores de la IA (cómo se corrigieron)

| # | Error / comportamiento IA | Corrección humana / resultado |
|---|---------------------------|------------------------------|
| 1 | **README decía FastAPI** para la API hospitalaria cuando el código es **Flask** (`api/app/`). | Día 9: README y memoria unifican Flask (API) + FastAPI (solo `ml/`). |
| 2 | **Pestañas Radiografías/Resumen vacías** en UI: la IA añadió clase `hidden` (`display:none !important`) que anulaba `.panel.active`. | Usuario reportó UI rota; fix en `switchPanel()` — quitar `hidden` al activar panel (`app.js`). |
| 3 | **Dashboard Streamlit** no arrancaba en Docker: `streamlit` no en PATH del contenedor. | Cambio a `python3 -m streamlit` en Dockerfile; `docker compose build --no-cache dashboard`. |
| 4 | **Datos demo** en formularios (`sites` placeholder) mezclados con catálogo real. | Migración `05-remove-demo-rows.sql` y validadores sin placeholders. |
| 5 | **Diario Día 8** generado en formato resumido sin tabla de metadatos ni hash de commit. | Reformato al estilo `2026-05-07.md` antes del commit. |
| 6 | La IA sugirió **rehacer D7** al implementar D8; el usuario solo pidió robustez incremental. | Se extendió pipeline/watcher sin duplicar dashboard. |

#### Archivos creados / modificados (Día 9)

- `docs/memoria-tecnica.md`, `docs/diagramas.md`, `docs/etica.md`
- `docs/specs/documentacion-d9.md`, `README.md`
- `docs/diario-ia/INDEX.md`, `docs/specs/BACKLOG.md`

#### Reflexión

La documentación obliga a **alinear narrativa y código** (Flask vs FastAPI fue un desajuste típico de vibe coding). Registrar errores (UI, Docker) demuestra supervisión humana, requisito del encargo. Los escenarios de Prompt Injection se documentan aunque el MVP use CNN determinista, porque el hospital podría añadir LLM en informes o chatbot.

#### Commit

- `14c62a2` — `docs: Día 9 — memoria técnica, diagramas, ética y README` (2026-05-09)

---
