# Diario IA — 2026-05-01

Herramienta: **Cursor**

---

### Entrada 001 — Infra Docker y stack compose

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-01 |
| Herramienta | Cursor |
| Resultado | parcial (acierto tras corrección) |
| Spec SDD | — (día 1 sin spec formal; backlog D1-*) |

#### Prompt del usuario

```
docker-compose up -d — verificar que funciona el stack
```

(Sesión ampliada: levantar stack completo, commit 21:00, fix mount callback, commit 21:15, push.)

#### Respuesta de la IA (resumen)

- Configuración `docker-compose.yml` con Postgres, MinIO, Spark, Airflow, API, ML, dashboard, pipeline.
- Builds correctos; en macOS falló el arranque con `mount callback failed` (bug Docker Desktop).
- Workaround: `docker compose start` + `docker compose up -d dashboard`; fix en compose (healthcheck API, dashboard espera API healthy).
- Commits con fechas solicitadas y push a GitHub.

#### Corrección

| Qué falló | Cómo se corrigió |
|-----------|------------------|
| `mount callback failed ... bad message` al crear muchos contenedores en paralelo | Reinicio Docker Desktop; arranque escalonado; healthcheck en API; `depends_on` dashboard → API healthy; comentarios de recuperación en compose |

**Prompt de corrección:**

```
docker compose down && docker compose up -d --build
(sigue fallando mount callback)
```

#### Archivos afectados

- `docker-compose.yml`
- `.env.example`, `README.md`, `docs/architecture.md`
- `infra/postgres/init-airflow-db.sql`
- `airflow/` (dags, logs, plugins)

#### Reflexión

La IA aceleró el compose multi-servicio y la documentación de arquitectura. La limitación principal fue un bug del entorno (Docker Desktop en macOS), no del YAML; hizo falta iterar con comandos manuales y conocimiento del error. Estimación: ~2–3 h ahorradas en boilerplate; ~30 min perdidos en debugging del daemon.

---

### Entrada 002 — Diario IA, SDD y backlog del proyecto

| Campo | Valor |
|-------|-------|
| Fecha | 2026-05-01 |
| Herramienta | Cursor |
| Resultado | acierto |
| Spec SDD | — (meta-documentación; ítems D1-08 en BACKLOG) |

#### Prompt del usuario

```
Crear Diario IA (prompts, correcciones, respuestas), preguntar tras cada prompt si guardar,
skill/agente que lea el proyecto, SDD según enunciado, tareas hechas y por hacer.
```

#### Respuesta de la IA (resumen)

- Skills Cursor: `@diario-ia` (flujo de guardado con confirmación) y `@salle-sdd` (specs + backlog).
- Estructura `docs/diario-ia/` (entradas por día, índice) y `docs/specs/` (plantilla SDD, BACKLOG.md).
- `AGENTS.md` como guía del repositorio para el agente.
- Entrada 001 del día ya existía (Docker); entrada 002 documenta esta sesión.

#### Corrección

No aplica.

#### Archivos afectados

- `.cursor/skills/diario-ia/SKILL.md`
- `.cursor/skills/diario-ia/entry-template.md`
- `.cursor/skills/salle-sdd/SKILL.md`
- `.cursor/skills/salle-sdd/spec-template.md`
- `docs/diario-ia/README.md`, `INDEX.md`, `entradas/2026-05-01.md`
- `docs/specs/README.md`, `BACKLOG.md`, `_TEMPLATE.md`
- `AGENTS.md`

#### Reflexión

Centralizar SDD y diario en el repo alinea el flujo con el encargo antes de escribir más código. La pregunta explícita «¿guardar?» evita documentar sin consentimiento y facilita la memoria técnica final.

---
